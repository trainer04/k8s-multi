# Sync configuration
sync:
  toHost:
    pods:
      enabled: true

# StatefulSet persistence configuration
statefulSet:
  persistence:
    volumeClaim:
      enabled: true
      existingClaim: vcluster-data
      size: 5Gi