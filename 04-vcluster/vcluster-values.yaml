# vCluster configuration without persistent storage
syncer:
  storage:
    persistence: false

controlPlane:
  backingStore:
    etcd:
      embedded:
        enabled: true

sync:
  toHost:
    pods:
      enabled: true