apiVersion: v1
kind: ResourceQuota
metadata:
  name: digital-team-quota
  namespace: digital-team
spec:
  hard:
    requests.cpu: "16"
    requests.memory: "32Gi"
    limits.cpu: "32"
    limits.memory: "64Gi"
    pods: "100"
    services: "20"
    persistentvolumeclaims: "10"
